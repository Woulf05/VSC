#include <cs50.h>
#include <stdio.h>

// calculates the population size after a certain amount of years

int main(void)
{
    int n;
    do
    {
        n = get_int("Start size: ");
    }
    while (n < 9);

    int y = get_int("Years: ");
    int e = n + n / 3 - n / 4;

    printf("End size: %i\n", e);
}
